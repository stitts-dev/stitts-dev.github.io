# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: true
  weight: 4
  showOnNavbar: true
  # Can optionally hide the title in sections
  hideTitle: true
  numShow: 1 # Can optionally increase the number of posts to display (default: 3)
  showMoreButton: true #Can optionally show 'More Posts' button (default: false)
# no additional configuration is required
